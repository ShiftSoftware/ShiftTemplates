{
  "name": "stockplusplus-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "End-to-end browser tests for the attention feature in StockPlusPlus, driven with puppeteer-core against a live API + Blazor WASM Web. Mirrors the ADP Surveys e2e harness pattern (step/assert/sql helpers, seed -> test -> cleanup).",
  "scripts": {
    "test:e2e": "tsx e2e.ts",
    "test:e2e:headed": "tsx e2e.ts --headed",
    "probe": "tsx probe.ts",
    "probe:headed": "tsx probe.ts --headed",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "puppeteer-core": "^24.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
