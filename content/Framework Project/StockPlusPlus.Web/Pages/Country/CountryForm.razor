@attribute [Route($"/{nameof(CountryForm)}/{{Key?}}")]
@using StockPlusPlus.WebComponents.Pages.Country

<StockPlusPlus.WebComponents.Pages.Country.CountryForm Name="@Name" Key="@Key"/>

@code {
    [Parameter]
    public string? Name { get; set; }

    [Parameter]
    public string? Key { get; set; }
}