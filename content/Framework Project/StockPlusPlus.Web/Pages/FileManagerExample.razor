@attribute [Route($"/filemanager")]

@if (Root is not null)
{
    <FileExplorer Root="@Root" RootAliasName="abc"></FileExplorer>
}

@code {
    [SupplyParameterFromQuery]
    public string Root { get; set; } = default!;
}