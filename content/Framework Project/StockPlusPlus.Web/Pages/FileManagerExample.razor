@attribute [Route($"/filemanager/{{Root?}}")]

@if (Root is not null)
{
    <FileManager FileManagerRoot="@Root"></FileManager>
}


@code {
    [Parameter]
    public string Root { get; set; } = default!;
}