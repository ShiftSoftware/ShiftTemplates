@attribute [Route($"/FileExplorer")]

<FileExplorer @key="Root" Root="@Root" RootAliasName="@Root?.Split('/').LastOrDefault()"></FileExplorer>

@code {
    [SupplyParameterFromQuery]
    public string? Root { get; set; } = string.Empty;
}