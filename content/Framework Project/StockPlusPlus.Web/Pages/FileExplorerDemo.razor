@attribute [Route($"/FileExplorer")]

<FileExplorer MaxFileSizeInMegaBytes="64" MaxUploadFileCount="4" @key="Root" Root="@Root" RootAliasName="@Root?.Split('/').LastOrDefault()"></FileExplorer>

@code {
    [SupplyParameterFromQuery]
    public string? Root { get; set; } = string.Empty;
}