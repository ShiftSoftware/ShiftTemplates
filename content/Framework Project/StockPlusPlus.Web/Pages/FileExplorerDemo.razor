@attribute [Route($"/FileExplorer")]

<FileExplorer ContainerName="identity" ShowThumbnails MaxFileSizeInMegaBytes="64" Height="600px" @key="Root" Root="@Root" RootAliasName="@Root?.Split('/', StringSplitOptions.RemoveEmptyEntries).LastOrDefault()"></FileExplorer>

@code {
    [SupplyParameterFromQuery]
    public string? Root { get; set; } = string.Empty;
}