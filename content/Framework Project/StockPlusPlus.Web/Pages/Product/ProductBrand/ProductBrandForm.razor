@attribute [Route($"/{nameof(ProductBrandForm)}/{{Key?}}")]

@using StockPlusPlus.Shared.DTOs.Product.ProductBrand
@using StockPlusPlus.Shared.Enums.Product

@inherits ShiftForm<ProductBrandForm, ProductBrandDTO>

<ShiftEntityForm @bind-Mode="Mode"
                 @bind-Value="TheItem"
                 @ref="FormContainer"
                 Action="ProductBrand"
                 Title="ProductBrand"
                 TypeAuthAction="StockPlusPlus.Shared.ActionTrees.StockActionTrees.ProductBrand"
                 @bind-Key="@Key">

    <LocalizedTextField @bind-Value="TheItem.Name"
                        For="() => TheItem.Name"
                        Label="Name" />

    @* <MudTextFieldExtended OnlyValidateIfDirty="true"
    Label="Name"
    @bind-Value="TheItem.Name"
    For="@(() => TheItem.Name)" /> *@

    @if (Mode == FormModes.View)
    {
        <br />
        <br />

        <StockPlusPlus.Web.Pages.Product.Product.ProductList ProductBrandID="@TheItem.ID" />
    }
</ShiftEntityForm>


@code {
    [Parameter]
    public string? BrandName { get; set; }

    protected override void OnInitialized()
    {
        if (!string.IsNullOrWhiteSpace(BrandName))
        {
            TheItem.Name = BrandName;
        }
    }
}